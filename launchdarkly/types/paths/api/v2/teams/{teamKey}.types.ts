// This code was automatically generated from an OpenAPI description.
// Do not edit this file. Edit the OpenAPI file instead.
// For more information, see https://github.com/pmcelhaney/counterfact/blob/main/docs/faq.md

import type { WideOperationArgument } from "../../../../../counterfact-types/index.ts";
import type { OmitValueWhenNever } from "../../../../../counterfact-types/index.ts";
import type { MaybePromise } from "../../../../../counterfact-types/index.ts";
import type { COUNTERFACT_RESPONSE } from "../../../../../counterfact-types/index.ts";
import type { Context } from "../../../../../routes/_.context.ts";
import type { ResponseBuilderFactory } from "../../../../../counterfact-types/index.ts";
import type { Team } from "../../../../components/schemas/Team.js";
import type { InvalidRequestErrorRep } from "../../../../components/schemas/InvalidRequestErrorRep.js";
import type { UnauthorizedErrorRep } from "../../../../components/schemas/UnauthorizedErrorRep.js";
import type { ForbiddenErrorRep } from "../../../../components/schemas/ForbiddenErrorRep.js";
import type { NotFoundErrorRep } from "../../../../components/schemas/NotFoundErrorRep.js";
import type { MethodNotAllowedErrorRep } from "../../../../components/schemas/MethodNotAllowedErrorRep.js";
import type { RateLimitedErrorRep } from "../../../../components/schemas/RateLimitedErrorRep.js";
import type { teamPatchInput } from "../../../../components/schemas/teamPatchInput.js";
import type { StatusConflictErrorRep } from "../../../../components/schemas/StatusConflictErrorRep.js";

/**
 * Fetch a team by key.
 *
 * ### Expanding the teams response
 * LaunchDarkly supports four fields for expanding the "Get team" response. By default, these fields are **not** included in the response.
 *
 * To expand the response, append the `expand` query parameter and add a comma-separated list with any of the following fields:
 *
 * * `members` includes the total count of members that belong to the team.
 * * `roles` includes a paginated list of the custom roles that you have assigned to the team.
 * * `projects` includes a paginated list of the projects that the team has any write access to.
 * * `maintainers` includes a paginated list of the maintainers that you have assigned to the team.
 *
 * For example, `expand=members,roles` includes the `members` and `roles` fields in the response.
 *
 */
export type getTeam = (
  $: OmitValueWhenNever<{
    query: getTeam_Query;
    querystring: never;
    path: getTeam_Path;
    headers: getTeam_Headers;
    cookie: never;
    body: never;
    context: Context;
    response: ResponseBuilderFactory<{
      200: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: Team;
          };
        };
        examples: {};
      };
      400: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: InvalidRequestErrorRep;
          };
        };
        examples: {};
      };
      401: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: UnauthorizedErrorRep;
          };
        };
        examples: {};
      };
      403: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: ForbiddenErrorRep;
          };
        };
        examples: {};
      };
      404: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: NotFoundErrorRep;
          };
        };
        examples: {};
      };
      405: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: MethodNotAllowedErrorRep;
          };
        };
        examples: {};
      };
      429: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: RateLimitedErrorRep;
          };
        };
        examples: {};
      };
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    auth: { apiKey: string };
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

/**
 * Perform a partial update to a team. Updating a team uses the semantic patch format.
 *
 * To make a semantic patch request, you must append `domain-model=launchdarkly.semanticpatch` to your `Content-Type` header. To learn more, read [Updates using semantic patch](/reference#updates-using-semantic-patch).
 *
 * ### Instructions
 *
 * Semantic patch requests support the following `kind` instructions for updating teams.
 *
 * <details>
 * <summary>Click to expand instructions for <strong>updating teams</strong></summary>
 *
 * #### addCustomRoles
 *
 * Adds custom roles to the team. Team members will have these custom roles granted to them.
 *
 * ##### Parameters
 *
 * - `values`: List of custom role keys.
 *
 * Here's an example:
 *
 * ```json
 * {
 *   "instructions": [{
 *     "kind": "addCustomRoles",
 *     "values": [ "example-custom-role" ]
 *   }]
 * }
 * ```
 *
 * #### removeCustomRoles
 *
 * Removes custom roles from the team. The app will no longer grant these custom roles to the team members.
 *
 * ##### Parameters
 *
 * - `values`: List of custom role keys.
 *
 * Here's an example:
 *
 * ```json
 * {
 *   "instructions": [{
 *     "kind": "removeCustomRoles",
 *     "values": [ "example-custom-role" ]
 *   }]
 * }
 * ```
 *
 * #### addMembers
 *
 * Adds members to the team.
 *
 * ##### Parameters
 *
 * - `values`: List of member IDs to add.
 *
 * Here's an example:
 *
 * ```json
 * {
 *   "instructions": [{
 *     "kind": "addMembers",
 *     "values": [ "1234a56b7c89d012345e678f", "507f1f77bcf86cd799439011" ]
 *   }]
 * }
 * ```
 *
 * #### removeMembers
 *
 * Removes members from the team.
 *
 * ##### Parameters
 *
 * - `values`: List of member IDs to remove.
 *
 * Here's an example:
 *
 * ```json
 * {
 *   "instructions": [{
 *     "kind": "removeMembers",
 *     "values": [ "1234a56b7c89d012345e678f", "507f1f77bcf86cd799439011" ]
 *   }]
 * }
 * ```
 *
 * #### replaceMembers
 *
 * Replaces the existing members of the team with the new members.
 *
 * ##### Parameters
 *
 * - `values`: List of member IDs of the new members.
 *
 * Here's an example:
 *
 * ```json
 * {
 *   "instructions": [{
 *     "kind": "replaceMembers",
 *     "values": [ "1234a56b7c89d012345e678f", "507f1f77bcf86cd799439011" ]
 *   }]
 * }
 * ```
 *
 * #### addPermissionGrants
 *
 * Adds permission grants to members for the team. For example, a permission grant could allow a member to act as a team maintainer. A permission grant may have either an `actionSet` or a list of `actions` but not both at the same time. The members do not have to be team members to have a permission grant for the team.
 *
 * ##### Parameters
 *
 * - `actionSet`: Name of the action set.
 * - `actions`: List of actions.
 * - `memberIDs`: List of member IDs.
 *
 * Here's an example:
 *
 * ```json
 * {
 *   "instructions": [{
 *     "kind": "addPermissionGrants",
 *     "actions": [ "updateTeamName", "updateTeamDescription" ],
 *     "memberIDs": [ "1234a56b7c89d012345e678f", "507f1f77bcf86cd799439011" ]
 *   }]
 * }
 * ```
 *
 * #### removePermissionGrants
 *
 * Removes permission grants from members for the team. A permission grant may have either an `actionSet` or a list of `actions` but not both at the same time. The `actionSet` and `actions` must match an existing permission grant.
 *
 * ##### Parameters
 *
 * - `actionSet`: Name of the action set.
 * - `actions`: List of actions.
 * - `memberIDs`: List of member IDs.
 *
 * Here's an example:
 *
 * ```json
 * {
 *   "instructions": [{
 *     "kind": "removePermissionGrants",
 *     "actions": [ "updateTeamName", "updateTeamDescription" ],
 *     "memberIDs": [ "1234a56b7c89d012345e678f", "507f1f77bcf86cd799439011" ]
 *   }]
 * }
 * ```
 *
 * #### updateDescription
 *
 * Updates the description of the team.
 *
 * ##### Parameters
 *
 * - `value`: The new description.
 *
 * Here's an example:
 *
 * ```json
 * {
 *   "instructions": [{
 *     "kind": "updateDescription",
 *     "value": "Updated team description"
 *   }]
 * }
 * ```
 *
 * #### updateName
 *
 * Updates the name of the team.
 *
 * ##### Parameters
 *
 * - `value`: The new name.
 *
 * Here's an example:
 *
 * ```json
 * {
 *   "instructions": [{
 *     "kind": "updateName",
 *     "value": "Updated team name"
 *   }]
 * }
 * ```
 *
 * </details>
 *
 * ### Expanding the teams response
 * LaunchDarkly supports four fields for expanding the "Update team" response. By default, these fields are **not** included in the response.
 *
 * To expand the response, append the `expand` query parameter and add a comma-separated list with any of the following fields:
 *
 * * `members` includes the total count of members that belong to the team.
 * * `roles` includes a paginated list of the custom roles that you have assigned to the team.
 * * `projects` includes a paginated list of the projects that the team has any write access to.
 * * `maintainers` includes a paginated list of the maintainers that you have assigned to the team.
 *
 * For example, `expand=members,roles` includes the `members` and `roles` fields in the response.
 *
 */
export type patchTeam = (
  $: OmitValueWhenNever<{
    query: patchTeam_Query;
    querystring: never;
    path: patchTeam_Path;
    headers: patchTeam_Headers;
    cookie: never;
    body: teamPatchInput;
    context: Context;
    response: ResponseBuilderFactory<{
      200: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: Team;
          };
        };
        examples: {};
      };
      400: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: InvalidRequestErrorRep;
          };
        };
        examples: {};
      };
      401: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: UnauthorizedErrorRep;
          };
        };
        examples: {};
      };
      404: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: NotFoundErrorRep;
          };
        };
        examples: {};
      };
      405: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: MethodNotAllowedErrorRep;
          };
        };
        examples: {};
      };
      409: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: StatusConflictErrorRep;
          };
        };
        examples: {};
      };
      429: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: RateLimitedErrorRep;
          };
        };
        examples: {};
      };
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    auth: { apiKey: string };
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

/**
 * Delete a team by key. To learn more, read [Delete a team](https://docs.launchdarkly.com/home/account/manage-teams#delete-a-team).
 */
export type deleteTeam = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: deleteTeam_Path;
    headers: deleteTeam_Headers;
    cookie: never;
    body: never;
    context: Context;
    response: ResponseBuilderFactory<{
      204: {
        headers: never;
        requiredHeaders: never;
        content: never;
        examples: {};
      };
      401: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: UnauthorizedErrorRep;
          };
        };
        examples: {};
      };
      404: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: NotFoundErrorRep;
          };
        };
        examples: {};
      };
      429: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: RateLimitedErrorRep;
          };
        };
        examples: {};
      };
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    auth: { apiKey: string };
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

export type getTeam_Query = {
  /**
   * A comma-separated list of properties that can reveal additional information in the response.
   */
  expand?: string;
};

export type getTeam_Path = {
  /**
   * The team key.
   */
  teamKey: string;
};

export type getTeam_Headers = { Authorization: string };

export type patchTeam_Query = {
  /**
   * A comma-separated list of properties that can reveal additional information in the response. Supported fields are explained above.
   */
  expand?: string;
};

export type patchTeam_Path = {
  /**
   * The team key
   */
  teamKey: string;
};

export type patchTeam_Headers = { Authorization: string };

export type deleteTeam_Path = {
  /**
   * The team key
   */
  teamKey: string;
};

export type deleteTeam_Headers = { Authorization: string };
