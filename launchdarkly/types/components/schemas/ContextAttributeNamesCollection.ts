import type { ContextAttributeNames } from "./ContextAttributeNames.js";

export type ContextAttributeNamesCollection = {
  /**
   * A collection of context attribute name data grouped by kind.
   */
  items: Array<ContextAttributeNames>;
};
