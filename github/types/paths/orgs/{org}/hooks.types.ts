// This code was automatically generated from an OpenAPI description.
// Do not edit this file. Edit the OpenAPI file instead.
// For more information, see https://github.com/pmcelhaney/counterfact/blob/main/docs/faq.md

import type { WideOperationArgument } from "../../../../counterfact-types/index.ts";
import type { OmitValueWhenNever } from "../../../../counterfact-types/index.ts";
import type { MaybePromise } from "../../../../counterfact-types/index.ts";
import type { COUNTERFACT_RESPONSE } from "../../../../counterfact-types/index.ts";
import type { Context } from "../../../../routes/_.context.ts";
import type { ResponseBuilderFactory } from "../../../../counterfact-types/index.ts";
import type { org_hook } from "../../../components/schemas/org-hook.js";
import type { not_found } from "../../../#/components/responses/not_found.js";
import type { webhook_config_url } from "../../../components/schemas/webhook-config-url.js";
import type { webhook_config_content_type } from "../../../components/schemas/webhook-config-content-type.js";
import type { webhook_config_secret } from "../../../components/schemas/webhook-config-secret.js";
import type { webhook_config_insecure_ssl } from "../../../components/schemas/webhook-config-insecure-ssl.js";
import type { validation_failed } from "../../../#/components/responses/validation_failed.js";

/**
 * List webhooks for an organization.
 *
 * The authenticated user must be an organization owner to use this endpoint.
 *
 * OAuth app tokens and personal access tokens (classic) need `admin:org_hook` scope. OAuth apps cannot list, view, or edit
 * webhooks that they did not create and users cannot list, view, or edit webhooks that were created by OAuth apps.
 */
export type orgsListWebhooks = (
  $: OmitValueWhenNever<{
    query: orgsListWebhooks_Query;
    querystring: never;
    path: orgsListWebhooks_Path;
    headers: never;
    cookie: never;
    body: never;
    context: Context;
    response: ResponseBuilderFactory<{
      200: {
        headers: {
          Link: { schema: string };
        };
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: Array<org_hook>;
          };
        };
        examples: {
          default: unknown;
        };
      };
      404: not_found;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

/**
 * Create a hook that posts payloads in JSON format.
 *
 * You must be an organization owner to use this endpoint.
 *
 * OAuth app tokens and personal access tokens (classic) need `admin:org_hook` scope. OAuth apps cannot list, view, or
 * edit webhooks that they did not create and users cannot list, view, or edit webhooks that were created by OAuth apps.
 */
export type orgsCreateWebhook = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: orgsCreateWebhook_Path;
    headers: never;
    cookie: never;
    body: {
      /**
       * Must be passed as "web".
       */
      name: string;
      /**
       * Key/value pairs to provide settings for this webhook.
       */
      config: {
        url: webhook_config_url;
        content_type?: webhook_config_content_type;
        secret?: webhook_config_secret;
        insecure_ssl?: webhook_config_insecure_ssl;
        /**
         * @example "\"kdaigle\""
         */
        username?: string;
        /**
         * @example "\"password\""
         */
        password?: string;
      };
      /**
       * Determines what [events](https://docs.github.com/webhooks/event-payloads) the hook is triggered for. Set to `["*"]` to receive all possible events.
       * @default ["push"]
       */
      events?: Array<string>;
      /**
       * Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications.
       * @default true
       */
      active?: boolean;
    };
    context: Context;
    response: ResponseBuilderFactory<{
      201: {
        headers: {
          Location: { schema: string };
        };
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: org_hook;
          };
        };
        examples: {
          default: unknown;
        };
      };
      404: not_found;
      422: validation_failed;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

export type orgsListWebhooks_Query = { per_page?: number; page?: number };

export type orgsListWebhooks_Path = { org: string };

export type orgsCreateWebhook_Path = { org: string };
