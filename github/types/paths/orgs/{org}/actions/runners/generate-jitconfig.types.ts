// This code was automatically generated from an OpenAPI description.
// Do not edit this file. Edit the OpenAPI file instead.
// For more information, see https://github.com/pmcelhaney/counterfact/blob/main/docs/faq.md

import type { WideOperationArgument } from "../../../../../../counterfact-types/index.ts";
import type { OmitValueWhenNever } from "../../../../../../counterfact-types/index.ts";
import type { MaybePromise } from "../../../../../../counterfact-types/index.ts";
import type { COUNTERFACT_RESPONSE } from "../../../../../../counterfact-types/index.ts";
import type { Context } from "../../../../../../routes/_.context.ts";
import type { ResponseBuilderFactory } from "../../../../../../counterfact-types/index.ts";
import type { actions_runner_jitconfig } from "../../../../../#/components/responses/actions_runner_jitconfig.js";
import type { not_found } from "../../../../../#/components/responses/not_found.js";
import type { conflict } from "../../../../../#/components/responses/conflict.js";
import type { validation_failed_simple } from "../../../../../#/components/responses/validation_failed_simple.js";

/**
 * Generates a configuration that can be passed to the runner application at startup.
 *
 * The authenticated user must have admin access to the organization.
 *
 * OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
 */
export type actionsGenerateRunnerJitconfigForOrg = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: actionsGenerateRunnerJitconfigForOrg_Path;
    headers: never;
    cookie: never;
    body: {
      /**
       * The name of the new runner.
       */
      name: string;
      /**
       * The ID of the runner group to register the runner to.
       */
      runner_group_id: number;
      /**
       * The names of the custom labels to add to the runner. **Minimum items**: 1. **Maximum items**: 100.
       */
      labels: Array<string>;
      /**
       * The working directory to be used for job execution, relative to the runner install directory.
       * @default "_work"
       */
      work_folder?: string;
    };
    context: Context;
    response: ResponseBuilderFactory<{
      201: actions_runner_jitconfig;
      404: not_found;
      409: conflict;
      422: validation_failed_simple;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

export type actionsGenerateRunnerJitconfigForOrg_Path = { org: string };
