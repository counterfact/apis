// This code was automatically generated from an OpenAPI description.
// Do not edit this file. Edit the OpenAPI file instead.
// For more information, see https://github.com/pmcelhaney/counterfact/blob/main/docs/faq.md

import type { WideOperationArgument } from "../../../../../../../counterfact-types/index.ts";
import type { OmitValueWhenNever } from "../../../../../../../counterfact-types/index.ts";
import type { MaybePromise } from "../../../../../../../counterfact-types/index.ts";
import type { COUNTERFACT_RESPONSE } from "../../../../../../../counterfact-types/index.ts";
import type { Context } from "../../../../../../../routes/_.context.ts";
import type { ResponseBuilderFactory } from "../../../../../../../counterfact-types/index.ts";
import type { actions_runner_labels } from "../../../../../../#/components/responses/actions_runner_labels.js";
import type { not_found } from "../../../../../../#/components/responses/not_found.js";
import type { validation_failed_simple } from "../../../../../../#/components/responses/validation_failed_simple.js";
import type { actions_runner_labels_readonly } from "../../../../../../#/components/responses/actions_runner_labels_readonly.js";

/**
 * Lists all labels for a self-hosted runner configured in an organization.
 *
 * Authenticated users must have admin access to the organization to use this endpoint.
 *
 * OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
 */
export type actionsListLabelsForSelfHostedRunnerForOrg = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: actionsListLabelsForSelfHostedRunnerForOrg_Path;
    headers: never;
    cookie: never;
    body: never;
    context: Context;
    response: ResponseBuilderFactory<{
      200: actions_runner_labels;
      404: not_found;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

/**
 * Adds custom labels to a self-hosted runner configured in an organization.
 *
 * Authenticated users must have admin access to the organization to use this endpoint.
 *
 * OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
 */
export type actionsAddCustomLabelsToSelfHostedRunnerForOrg = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: actionsAddCustomLabelsToSelfHostedRunnerForOrg_Path;
    headers: never;
    cookie: never;
    body: {
      /**
       * The names of the custom labels to add to the runner.
       */
      labels: Array<string>;
    };
    context: Context;
    response: ResponseBuilderFactory<{
      200: actions_runner_labels;
      404: not_found;
      422: validation_failed_simple;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

/**
 * Remove all previous custom labels and set the new custom labels for a specific
 * self-hosted runner configured in an organization.
 *
 * Authenticated users must have admin access to the organization to use this endpoint.
 *
 * OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
 */
export type actionsSetCustomLabelsForSelfHostedRunnerForOrg = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: actionsSetCustomLabelsForSelfHostedRunnerForOrg_Path;
    headers: never;
    cookie: never;
    body: {
      /**
       * The names of the custom labels to set for the runner. You can pass an empty array to remove all custom labels.
       */
      labels: Array<string>;
    };
    context: Context;
    response: ResponseBuilderFactory<{
      200: actions_runner_labels;
      404: not_found;
      422: validation_failed_simple;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

/**
 * Remove all custom labels from a self-hosted runner configured in an
 * organization. Returns the remaining read-only labels from the runner.
 *
 * Authenticated users must have admin access to the organization to use this endpoint.
 *
 * OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
 */
export type actionsRemoveAllCustomLabelsFromSelfHostedRunnerForOrg = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: actionsRemoveAllCustomLabelsFromSelfHostedRunnerForOrg_Path;
    headers: never;
    cookie: never;
    body: never;
    context: Context;
    response: ResponseBuilderFactory<{
      200: actions_runner_labels_readonly;
      404: not_found;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

export type actionsListLabelsForSelfHostedRunnerForOrg_Path = {
  org: string;
  runner_id: number;
};

export type actionsAddCustomLabelsToSelfHostedRunnerForOrg_Path = {
  org: string;
  runner_id: number;
};

export type actionsSetCustomLabelsForSelfHostedRunnerForOrg_Path = {
  org: string;
  runner_id: number;
};

export type actionsRemoveAllCustomLabelsFromSelfHostedRunnerForOrg_Path = {
  org: string;
  runner_id: number;
};
