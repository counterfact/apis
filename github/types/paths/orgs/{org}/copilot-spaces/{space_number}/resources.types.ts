// This code was automatically generated from an OpenAPI description.
// Do not edit this file. Edit the OpenAPI file instead.
// For more information, see https://github.com/pmcelhaney/counterfact/blob/main/docs/faq.md

import type { WideOperationArgument } from "../../../../../../counterfact-types/index.ts";
import type { OmitValueWhenNever } from "../../../../../../counterfact-types/index.ts";
import type { MaybePromise } from "../../../../../../counterfact-types/index.ts";
import type { COUNTERFACT_RESPONSE } from "../../../../../../counterfact-types/index.ts";
import type { Context } from "../../../../../../routes/_.context.ts";
import type { ResponseBuilderFactory } from "../../../../../../counterfact-types/index.ts";
import type { copilot_space_resource } from "../../../../../components/schemas/copilot-space-resource.js";
import type { forbidden } from "../../../../../#/components/responses/forbidden.js";
import type { not_found } from "../../../../../#/components/responses/not_found.js";
import type { validation_failed } from "../../../../../#/components/responses/validation_failed.js";

/**
 * Lists all resources attached to a specific Copilot Space owned by an organization.
 * The authenticated user must have appropriate permissions to view the space.
 *
 * OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
 *
 * Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space.
 */
export type copilotSpacesListResourcesForOrg = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: copilotSpacesListResourcesForOrg_Path;
    headers: never;
    cookie: never;
    body: never;
    context: Context;
    response: ResponseBuilderFactory<{
      200: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: {
              /**
               * The list of resources attached to this Copilot Space.
               */
              resources: Array<copilot_space_resource>;
            };
          };
        };
        examples: {
          default: unknown;
        };
      };
      403: forbidden;
      404: not_found;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

/**
 * Creates a new resource in a specific Copilot Space owned by an organization.
 * The authenticated user must have write permissions on the space.
 *
 * The following resource types are supported: `repository`, `github_file`, `free_text`, `github_issue`, `github_pull_request`.
 * The `uploaded_text_file` and `media_content` types are not supported via this endpoint.
 *
 * For `github_file` resources, if a resource with the same repository, file path, and SHA already exists, the existing resource is returned with a `200` status.
 *
 * OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
 *
 * Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space, including the resource being created.
 */
export type copilotSpacesCreateResourceForOrg = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: copilotSpacesCreateResourceForOrg_Path;
    headers: never;
    cookie: never;
    body: {
      /**
       * The type of resource to create.
       */
      resource_type:
        | "repository"
        | "github_file"
        | "free_text"
        | "github_issue"
        | "github_pull_request";
      /**
       * Resource-specific metadata.
       */
      metadata: { [key: string]: unknown };
    };
    context: Context;
    response: ResponseBuilderFactory<{
      200: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: copilot_space_resource;
          };
        };
        examples: {
          default: unknown;
        };
      };
      201: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: copilot_space_resource;
          };
        };
        examples: {
          default: unknown;
        };
      };
      403: forbidden;
      404: not_found;
      422: validation_failed;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

export type copilotSpacesListResourcesForOrg_Path = {
  org: string;
  /**
   * The unique identifier of the Copilot Space.
   */
  space_number: number;
};

export type copilotSpacesCreateResourceForOrg_Path = {
  org: string;
  /**
   * The unique identifier of the Copilot Space.
   */
  space_number: number;
};
