// This code was automatically generated from an OpenAPI description.
// Do not edit this file. Edit the OpenAPI file instead.
// For more information, see https://github.com/pmcelhaney/counterfact/blob/main/docs/faq.md

import type { WideOperationArgument } from "../../../../../counterfact-types/index.ts";
import type { OmitValueWhenNever } from "../../../../../counterfact-types/index.ts";
import type { MaybePromise } from "../../../../../counterfact-types/index.ts";
import type { COUNTERFACT_RESPONSE } from "../../../../../counterfact-types/index.ts";
import type { Context } from "../../../../../routes/_.context.ts";
import type { ResponseBuilderFactory } from "../../../../../counterfact-types/index.ts";
import type { copilot_space } from "../../../../components/schemas/copilot-space.js";
import type { forbidden } from "../../../../#/components/responses/forbidden.js";
import type { not_found } from "../../../../#/components/responses/not_found.js";
import type { validation_failed } from "../../../../#/components/responses/validation_failed.js";

/**
 * Gets details about a specific Copilot Space owned by an organization. The authenticated user must have read access to the Space.
 *
 * Internal Spaces require the authenticated user to be a member of the organization or have been granted read permissions.
 *
 * OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
 *
 * Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space.
 */
export type copilotSpacesGetForOrg = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: copilotSpacesGetForOrg_Path;
    headers: never;
    cookie: never;
    body: never;
    context: Context;
    response: ResponseBuilderFactory<{
      200: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: copilot_space;
          };
        };
        examples: {
          default: unknown;
        };
      };
      403: forbidden;
      404: not_found;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

/**
 * Updates a Copilot Space owned by an organization. The authenticated user must have permissions to update spaces in the organization.
 *
 * Organization members with appropriate permissions can update Copilot Spaces owned by their organization.
 *
 * OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
 *
 * Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space, including any being added or updated.
 */
export type copilotSpacesUpdateForOrg = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: copilotSpacesUpdateForOrg_Path;
    headers: never;
    cookie: never;
    body: {
      /**
       * The name of the Copilot Space.
       * @example "Updated Team Planning Space"
       */
      name?: string;
      /**
       * A description of the Copilot Space.
       * @example "Updated organization space for team planning and coordination"
       */
      description?: string;
      /**
       * General instructions for the Copilot Space.
       * @example "Updated instructions to help the team with planning and coordination tasks"
       */
      general_instructions?: string;
      /**
       * The base role that determines default permissions for organization members. Changing this field requires admin permissions.
       * - `no_access`: No default access (default)
       * - `reader`: Organization members can read the space
       * - `writer`: Organization members can read and edit the space
       * - `admin`: Organization members have full admin access to the space
       */
      base_role?: "reader" | "writer" | "admin" | "no_access";
      /**
       * Resources to attach to the space.
       */
      resources_attributes?: Array<{
        /**
         * The type of resource.
         */
        resource_type?:
          | "repository"
          | "github_file"
          | "free_text"
          | "github_issue"
          | "github_pull_request"
          | "media_content"
          | "uploaded_text_file";
        /**
         * Metadata specific to the resource type.
         */
        metadata?: {
          /**
           * Repository ID for repository or file resources.
           */
          repository_id?: number;
          /**
           * File path for file resources.
           */
          file_path?: string;
          /**
           * Text content for free text resources.
           */
          text?: string;
          /**
           * Name for the resource.
           */
          name?: string;
          /**
           * Issue or PR number.
           */
          number?: number;
        };
      }>;
    };
    context: Context;
    response: ResponseBuilderFactory<{
      200: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: copilot_space;
          };
        };
        examples: {
          default: unknown;
        };
      };
      403: forbidden;
      404: not_found;
      422: validation_failed;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

/**
 * Deletes a Copilot Space owned by an organization. The authenticated user must have permissions to delete spaces in the organization.
 *
 * **Warning:** This action is permanent and cannot be undone. Deleting a Copilot Space will remove all associated resources and configurations.
 *
 * Organization members with appropriate permissions can delete Copilot Spaces owned by their organization.
 *
 * OAuth app tokens and personal access tokens (classic) need both the `read:org` and `repo` scopes to use this endpoint.
 *
 * Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space.
 */
export type copilotSpacesDeleteForOrg = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: copilotSpacesDeleteForOrg_Path;
    headers: never;
    cookie: never;
    body: never;
    context: Context;
    response: ResponseBuilderFactory<{
      204: {
        headers: never;
        requiredHeaders: never;
        content: never;
        examples: {};
      };
      404: not_found;
      422: validation_failed;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

export type copilotSpacesGetForOrg_Path = {
  org: string;
  /**
   * The unique identifier of the Copilot Space.
   */
  space_number: number;
};

export type copilotSpacesUpdateForOrg_Path = {
  org: string;
  /**
   * The unique identifier of the Copilot Space.
   */
  space_number: number;
};

export type copilotSpacesDeleteForOrg_Path = {
  org: string;
  /**
   * The unique identifier of the Copilot Space.
   */
  space_number: number;
};
