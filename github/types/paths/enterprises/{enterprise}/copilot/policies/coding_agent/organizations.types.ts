// This code was automatically generated from an OpenAPI description.
// Do not edit this file. Edit the OpenAPI file instead.
// For more information, see https://github.com/pmcelhaney/counterfact/blob/main/docs/faq.md

import type { WideOperationArgument } from "../../../../../../../counterfact-types/index.ts";
import type { OmitValueWhenNever } from "../../../../../../../counterfact-types/index.ts";
import type { MaybePromise } from "../../../../../../../counterfact-types/index.ts";
import type { COUNTERFACT_RESPONSE } from "../../../../../../../counterfact-types/index.ts";
import type { Context } from "../../../../../../../routes/_.context.ts";
import type { ResponseBuilderFactory } from "../../../../../../../counterfact-types/index.ts";
import type { no_content } from "../../../../../../#/components/responses/no_content.js";
import type { bad_request } from "../../../../../../#/components/responses/bad_request.js";

/**
 * Enables Copilot cloud agent for the specified organizations within the enterprise.
 *
 * The enterprise's coding agent policy must be set to `enabled_for_selected_orgs` before
 * using this endpoint. Organizations can be specified by login or matched via custom properties.
 *
 * Only organizations that have Copilot enabled and belong to the enterprise will be affected.
 *
 * Only enterprise owners can add organizations to the coding agent policy.
 *
 * OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot` or `admin:enterprise` scopes to use this endpoint.
 */
export type copilotAddOrganizationsToEnterpriseCodingAgentPolicy = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: copilotAddOrganizationsToEnterpriseCodingAgentPolicy_Path;
    headers: never;
    cookie: never;
    body: {
      /**
       * List of organization logins within the enterprise to enable Copilot cloud agent for.
       */
      organizations?: Array<string>;
      /**
       * List of custom property filters to match organizations. Organizations matching any of the specified property name/value pairs will be included. This is a one-time operation, setting the property on an organization in the future will not automatically update its coding agent policy.
       */
      custom_properties?: Array<{
        /**
         * The name of the custom property to filter by.
         */
        property_name: string;
        /**
         * The values of the custom property to match.
         */
        values: Array<string>;
      }>;
    };
    context: Context;
    response: ResponseBuilderFactory<{
      204: no_content;
      400: bad_request;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

/**
 * Disables Copilot cloud agent for the specified organizations within the enterprise.
 *
 * The enterprise's coding agent policy must be set to `enabled_for_selected_orgs` before
 * using this endpoint. Organizations can be specified by login or matched via custom properties.
 *
 * Only organizations that have Copilot enabled and belong to the enterprise will be affected.
 *
 * Only enterprise owners can remove organizations from the coding agent policy.
 *
 * OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot` or `admin:enterprise` scopes to use this endpoint.
 */
export type copilotRemoveOrganizationsFromEnterpriseCodingAgentPolicy = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: copilotRemoveOrganizationsFromEnterpriseCodingAgentPolicy_Path;
    headers: never;
    cookie: never;
    body: {
      /**
       * List of organization logins within the enterprise to disable Copilot cloud agent for.
       */
      organizations?: Array<string>;
      /**
       * List of custom property filters to match organizations. Organizations matching any of the specified property name/value pairs will be included. This is a one-time operation, setting the property on an organization in the future will not automatically update its coding agent policy.
       */
      custom_properties?: Array<{
        /**
         * The name of the custom property to filter by.
         */
        property_name: string;
        /**
         * The values of the custom property to match.
         */
        values: Array<string>;
      }>;
    };
    context: Context;
    response: ResponseBuilderFactory<{
      204: no_content;
      400: bad_request;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

export type copilotAddOrganizationsToEnterpriseCodingAgentPolicy_Path = {
  enterprise: string;
};

export type copilotRemoveOrganizationsFromEnterpriseCodingAgentPolicy_Path = {
  enterprise: string;
};
