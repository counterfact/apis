// This code was automatically generated from an OpenAPI description.
// Do not edit this file. Edit the OpenAPI file instead.
// For more information, see https://github.com/pmcelhaney/counterfact/blob/main/docs/faq.md

import type { WideOperationArgument } from "../../../../counterfact-types/index.ts";
import type { OmitValueWhenNever } from "../../../../counterfact-types/index.ts";
import type { MaybePromise } from "../../../../counterfact-types/index.ts";
import type { COUNTERFACT_RESPONSE } from "../../../../counterfact-types/index.ts";
import type { Context } from "../../../../routes/_.context.ts";
import type { ResponseBuilderFactory } from "../../../../counterfact-types/index.ts";
import type { enterprise_team } from "../../../components/schemas/enterprise-team.js";
import type { forbidden } from "../../../#/components/responses/forbidden.js";

/**
 * List all teams in the enterprise for the authenticated user
 */
export type enterpriseTeamsList = (
  $: OmitValueWhenNever<{
    query: enterpriseTeamsList_Query;
    querystring: never;
    path: enterpriseTeamsList_Path;
    headers: never;
    cookie: never;
    body: never;
    context: Context;
    response: ResponseBuilderFactory<{
      200: {
        headers: {
          Link: { schema: string };
        };
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: Array<enterprise_team>;
          };
        };
        examples: {
          default: unknown;
        };
      };
      403: forbidden;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

/**
 * To create an enterprise team, the authenticated user must be an owner of the enterprise.
 */
export type enterpriseTeamsCreate = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: enterpriseTeamsCreate_Path;
    headers: never;
    cookie: never;
    body: {
      /**
       * The name of the team.
       */
      name: string;
      /**
       * A description of the team.
       */
      description?: string;
      /**
       * Retired: this field is no longer supported.
       * Whether the enterprise team should be reflected in each organization.
       * This value cannot be set.
       *
       * @default "disabled"
       */
      sync_to_organizations?: "all" | "disabled";
      /**
       * Specifies which organizations in the enterprise should have access to this team. Can be one of `disabled`, `selected`, or `all`.
       * `disabled`: The team is not assigned to any organizations. This is the default when you create a new team.
       * `selected`: The team is assigned to specific organizations. You can then use the [add organization assignments API](https://docs.github.com/rest/enterprise-teams/enterprise-team-organizations#add-organization-assignments) endpoint.
       * `all`: The team is assigned to all current and future organizations in the enterprise.
       *
       * @default "disabled"
       */
      organization_selection_type?: "disabled" | "selected" | "all";
      /**
       * The ID of the IdP group to assign team membership with. You can get this value from the [REST API endpoints for SCIM](https://docs.github.com/rest/scim#list-provisioned-scim-groups-for-an-enterprise).
       */
      group_id?: string;
      /**
       * The notification setting the team is set to. The options are:
       *
       * * `notifications_enabled` - team members receive notifications when the team is @mentioned.
       * * `notifications_disabled` - no one receives notifications.
       *
       * Default: `notifications_enabled`
       *
       */
      notification_setting?: "notifications_enabled" | "notifications_disabled";
    };
    context: Context;
    response: ResponseBuilderFactory<{
      201: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: enterprise_team;
          };
        };
        examples: {
          default: unknown;
        };
      };
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

export type enterpriseTeamsList_Query = { per_page?: number; page?: number };

export type enterpriseTeamsList_Path = { enterprise: string };

export type enterpriseTeamsCreate_Path = { enterprise: string };
