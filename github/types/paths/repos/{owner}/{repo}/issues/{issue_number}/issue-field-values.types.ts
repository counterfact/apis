// This code was automatically generated from an OpenAPI description.
// Do not edit this file. Edit the OpenAPI file instead.
// For more information, see https://github.com/pmcelhaney/counterfact/blob/main/docs/faq.md

import type { WideOperationArgument } from "../../../../../../../counterfact-types/index.ts";
import type { OmitValueWhenNever } from "../../../../../../../counterfact-types/index.ts";
import type { MaybePromise } from "../../../../../../../counterfact-types/index.ts";
import type { COUNTERFACT_RESPONSE } from "../../../../../../../counterfact-types/index.ts";
import type { Context } from "../../../../../../../routes/_.context.ts";
import type { ResponseBuilderFactory } from "../../../../../../../counterfact-types/index.ts";
import type { issue_field_value } from "../../../../../../components/schemas/issue-field-value.js";
import type { moved_permanently } from "../../../../../../#/components/responses/moved_permanently.js";
import type { not_found } from "../../../../../../#/components/responses/not_found.js";
import type { gone } from "../../../../../../#/components/responses/gone.js";
import type { bad_request } from "../../../../../../#/components/responses/bad_request.js";
import type { forbidden } from "../../../../../../#/components/responses/forbidden.js";
import type { validation_failed } from "../../../../../../#/components/responses/validation_failed.js";
import type { service_unavailable } from "../../../../../../#/components/responses/service_unavailable.js";

/**
 * Lists all issue field values for an issue.
 */
export type issuesListIssueFieldValuesForIssue = (
  $: OmitValueWhenNever<{
    query: issuesListIssueFieldValuesForIssue_Query;
    querystring: never;
    path: issuesListIssueFieldValuesForIssue_Path;
    headers: never;
    cookie: never;
    body: never;
    context: Context;
    response: ResponseBuilderFactory<{
      200: {
        headers: {
          Link: { schema: string };
        };
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: Array<issue_field_value>;
          };
        };
        examples: {
          default: unknown;
        };
      };
      301: moved_permanently;
      404: not_found;
      410: gone;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

/**
 * Add custom field values to an issue. You can set values for organization-level issue fields that have been defined for the repository's organization.
 * Adding an empty array will clear all existing field values for the issue.
 *
 * This endpoint supports the following field data types:
 * - **`text`**: String values for text fields
 * - **`single_select`**: Option names for single-select fields (must match an existing option name)
 * - **`number`**: Numeric values for number fields
 * - **`date`**: ISO 8601 date strings for date fields
 *
 * Only users with push access to the repository can add issue field values. If you don't have the proper permissions, you'll receive a `403 Forbidden` response.
 *
 * This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
 * and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
 */
export type issuesAddIssueFieldValues = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: issuesAddIssueFieldValues_Path;
    headers: never;
    cookie: never;
    body: {
      /**
       * An array of issue field values to add to this issue. Each field value must include the field ID and the value to set.
       */
      issue_field_values?: Array<{
        /**
         * The ID of the issue field to set
         * @example 123
         */
        field_id: number;
        /**
         * The value to set for the field. The type depends on the field's data type:
         * - For text fields: provide a string value
         * - For single_select fields: provide the option name as a string (must match an existing option)
         * - For number fields: provide a numeric value
         * - For date fields: provide an ISO 8601 date string
         * @example "Critical"
         */
        value: string | number;
      }>;
    };
    context: Context;
    response: ResponseBuilderFactory<{
      200: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: Array<issue_field_value>;
          };
        };
        examples: {
          default: unknown;
        };
      };
      400: bad_request;
      403: forbidden;
      404: not_found;
      422: validation_failed;
      503: service_unavailable;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

/**
 * Set custom field values for an issue, replacing any existing values. You can set values for organization-level issue fields that have been defined for the repository's organization.
 *
 * This endpoint supports the following field data types:
 * - **`text`**: String values for text fields
 * - **`single_select`**: Option names for single-select fields (must match an existing option name)
 * - **`number`**: Numeric values for number fields
 * - **`date`**: ISO 8601 date strings for date fields
 *
 * This operation will replace all existing field values with the provided ones. If you want to add field values without replacing existing ones, use the `POST` endpoint instead.
 *
 * Only users with push access to the repository can set issue field values. If you don't have the proper permissions, you'll receive a `403 Forbidden` response.
 *
 * This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
 * and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
 */
export type issuesSetIssueFieldValues = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: issuesSetIssueFieldValues_Path;
    headers: never;
    cookie: never;
    body: {
      /**
       * An array of issue field values to set for this issue. Each field value must include the field ID and the value to set. All existing field values will be replaced.
       */
      issue_field_values?: Array<{
        /**
         * The ID of the issue field to set
         * @example 123
         */
        field_id: number;
        /**
         * The value to set for the field. The type depends on the field's data type:
         * - For text fields: provide a string value
         * - For single_select fields: provide the option name as a string (must match an existing option)
         * - For number fields: provide a numeric value
         * - For date fields: provide an ISO 8601 date string
         * @example "Critical"
         */
        value: string | number;
      }>;
    };
    context: Context;
    response: ResponseBuilderFactory<{
      200: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: Array<issue_field_value>;
          };
        };
        examples: {
          default: unknown;
        };
      };
      400: bad_request;
      403: forbidden;
      404: not_found;
      422: validation_failed;
      503: service_unavailable;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

export type issuesListIssueFieldValuesForIssue_Query = {
  per_page?: number;
  page?: number;
};

export type issuesListIssueFieldValuesForIssue_Path = {
  owner: string;
  repo: string;
  issue_number: number;
};

export type issuesAddIssueFieldValues_Path = {
  owner: string;
  repo: string;
  issue_number: number;
};

export type issuesSetIssueFieldValues_Path = {
  owner: string;
  repo: string;
  issue_number: number;
};
