// This code was automatically generated from an OpenAPI description.
// Do not edit this file. Edit the OpenAPI file instead.
// For more information, see https://github.com/pmcelhaney/counterfact/blob/main/docs/faq.md

import type { WideOperationArgument } from "../../../../counterfact-types/index.ts";
import type { OmitValueWhenNever } from "../../../../counterfact-types/index.ts";
import type { MaybePromise } from "../../../../counterfact-types/index.ts";
import type { COUNTERFACT_RESPONSE } from "../../../../counterfact-types/index.ts";
import type { Context } from "../../../../routes/_.context.ts";
import type { ResponseBuilderFactory } from "../../../../counterfact-types/index.ts";
import type { copilot_space } from "../../../components/schemas/copilot-space.js";
import type { forbidden } from "../../../#/components/responses/forbidden.js";
import type { not_found } from "../../../#/components/responses/not_found.js";
import type { validation_failed } from "../../../#/components/responses/validation_failed.js";

/**
 * Lists Copilot Spaces owned by a user. The authenticated user must have read access to the user's Copilot Spaces.
 *
 * Only Spaces that are readable by the authenticated user are returned. This includes the user's own spaces, and public user spaces when accessing another user's spaces.
 *
 * OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint.
 */
export type copilotSpacesListForUser = (
  $: OmitValueWhenNever<{
    query: copilotSpacesListForUser_Query;
    querystring: never;
    path: copilotSpacesListForUser_Path;
    headers: never;
    cookie: never;
    body: never;
    context: Context;
    response: ResponseBuilderFactory<{
      200: {
        headers: {
          Link: { schema: string };
        };
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: {
              /**
               * The list of Copilot Spaces on this page of results.
               */
              spaces: Array<copilot_space>;
            };
          };
        };
        examples: {
          default: unknown;
        };
      };
      403: forbidden;
      404: not_found;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

/**
 * Creates a new Copilot Space owned by a user. Only the authenticated user can create spaces for their own account.
 *
 * Users can create personal Copilot Spaces for their individual use.
 *
 * OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint.
 */
export type copilotSpacesCreateForUser = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: copilotSpacesCreateForUser_Path;
    headers: never;
    cookie: never;
    body: {
      /**
       * The name of the Copilot Space.
       * @example "My Development Space"
       */
      name: string;
      /**
       * A description of the Copilot Space.
       * @example "Personal space for development assistance"
       */
      description?: string;
      /**
       * General instructions for the Copilot Space.
       * @example "Help me with React development patterns and best practices"
       */
      general_instructions?: string;
      /**
       * The base role that determines default permissions for the space.
       * - `no_access`: No default access (default)
       * - `reader`: Makes the space publicly readable
       * Note: User spaces do not support writer or admin base roles.
       * @default "no_access"
       */
      base_role?: "reader" | "no_access";
      /**
       * Resources to attach to the space.
       */
      resources_attributes?: Array<{
        /**
         * The type of resource.
         */
        resource_type?:
          | "repository"
          | "github_file"
          | "free_text"
          | "github_issue"
          | "github_pull_request"
          | "media_content"
          | "uploaded_text_file";
        /**
         * Metadata specific to the resource type.
         */
        metadata?: {
          /**
           * Repository ID for repository or file resources.
           */
          repository_id?: number;
          /**
           * File path for file resources.
           */
          file_path?: string;
          /**
           * Text content for free text resources.
           */
          text?: string;
          /**
           * Name for the resource.
           */
          name?: string;
          /**
           * Issue or PR number.
           */
          number?: number;
        };
      }>;
    };
    context: Context;
    response: ResponseBuilderFactory<{
      201: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: copilot_space;
          };
        };
        examples: {
          default: unknown;
        };
      };
      403: forbidden;
      404: not_found;
      422: validation_failed;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

export type copilotSpacesListForUser_Query = {
  /**
   * The number of results per page (max 100).
   */
  per_page?: number;
  /**
   * A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor.
   */
  before?: string;
  /**
   * A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor.
   */
  after?: string;
};

export type copilotSpacesListForUser_Path = { username: string };

export type copilotSpacesCreateForUser_Path = { username: string };
