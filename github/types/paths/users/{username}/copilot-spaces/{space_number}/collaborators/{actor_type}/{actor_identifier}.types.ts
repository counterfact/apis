// This code was automatically generated from an OpenAPI description.
// Do not edit this file. Edit the OpenAPI file instead.
// For more information, see https://github.com/pmcelhaney/counterfact/blob/main/docs/faq.md

import type { WideOperationArgument } from "../../../../../../../../counterfact-types/index.ts";
import type { OmitValueWhenNever } from "../../../../../../../../counterfact-types/index.ts";
import type { MaybePromise } from "../../../../../../../../counterfact-types/index.ts";
import type { COUNTERFACT_RESPONSE } from "../../../../../../../../counterfact-types/index.ts";
import type { Context } from "../../../../../../../../routes/_.context.ts";
import type { ResponseBuilderFactory } from "../../../../../../../../counterfact-types/index.ts";
import type { copilot_space_collaborator } from "../../../../../../../components/schemas/copilot-space-collaborator.js";
import type { forbidden } from "../../../../../../../#/components/responses/forbidden.js";
import type { not_found } from "../../../../../../../#/components/responses/not_found.js";
import type { validation_failed } from "../../../../../../../#/components/responses/validation_failed.js";

/**
 * Updates the role of a collaborator for a specific Copilot Space owned by a user. The authenticated user must be the owner of the space or have admin access to the space.
 *
 * OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.
 */
export type copilotSpacesUpdateCollaboratorForUser = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: copilotSpacesUpdateCollaboratorForUser_Path;
    headers: never;
    cookie: never;
    body: {
      /**
       * The new role to grant to the collaborator. Use `no_access` to remove the collaborator.
       */
      role: "reader" | "writer" | "admin" | "no_access";
    };
    context: Context;
    response: ResponseBuilderFactory<{
      200: {
        headers: never;
        requiredHeaders: never;
        content: {
          "application/json": {
            schema: copilot_space_collaborator;
          };
        };
        examples: {
          default: unknown;
        };
      };
      204: {
        headers: never;
        requiredHeaders: never;
        content: never;
        examples: {};
      };
      403: forbidden;
      404: not_found;
      422: validation_failed;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

/**
 * Removes a collaborator from a specific Copilot Space owned by a user. The authenticated user must be the owner of the space or have admin access to the space.
 *
 * OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.
 */
export type copilotSpacesRemoveCollaboratorForUser = (
  $: OmitValueWhenNever<{
    query: never;
    querystring: never;
    path: copilotSpacesRemoveCollaboratorForUser_Path;
    headers: never;
    cookie: never;
    body: never;
    context: Context;
    response: ResponseBuilderFactory<{
      204: {
        headers: never;
        requiredHeaders: never;
        content: never;
        examples: {};
      };
      403: forbidden;
      404: not_found;
    }>;
    x: WideOperationArgument;
    proxy: (url: string) => COUNTERFACT_RESPONSE;
    user: never;
    delay: (milliseconds: number, maxMilliseconds?: number) => Promise<void>;
    version: never;
  }>,
) => MaybePromise<COUNTERFACT_RESPONSE>;

export type copilotSpacesUpdateCollaboratorForUser_Path = {
  username: string;
  /**
   * The unique identifier of the Copilot Space.
   */
  space_number: number;
  /**
   * The type of actor (must be `User` for user-owned spaces; `Team` will be rejected).
   */
  actor_type: "User" | "Team";
  /**
   * The username of the collaborator. The numeric user ID is also accepted.
   */
  actor_identifier: string;
};

export type copilotSpacesRemoveCollaboratorForUser_Path = {
  username: string;
  /**
   * The unique identifier of the Copilot Space.
   */
  space_number: number;
  /**
   * The type of actor (must be `User` for user-owned spaces; `Team` will be rejected).
   */
  actor_type: "User" | "Team";
  /**
   * The username of the collaborator. The numeric user ID is also accepted.
   */
  actor_identifier: string;
};
