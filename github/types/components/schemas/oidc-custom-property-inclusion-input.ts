/**
 * Input for creating an OIDC custom property inclusion
 */
export type oidc_custom_property_inclusion_input = {
  /**
   * The name of the custom property to include in the OIDC token
   */
  custom_property_name: string;
};
