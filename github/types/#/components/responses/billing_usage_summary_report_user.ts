import type { billing_usage_summary_report_user } from "../../../components/schemas/billing-usage-summary-report-user.js";

export type billing_usage_summary_report_user = {
  headers: never;
  requiredHeaders: never;
  content: {
    "application/json": {
      schema: billing_usage_summary_report_user;
    };
  };
  examples: {
    default: unknown;
  };
};
