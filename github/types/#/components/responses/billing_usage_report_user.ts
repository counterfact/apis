import type { billing_usage_report_user } from "../../../components/schemas/billing-usage-report-user.js";

export type billing_usage_report_user = {
  headers: never;
  requiredHeaders: never;
  content: {
    "application/json": {
      schema: billing_usage_report_user;
    };
  };
  examples: {
    default: unknown;
  };
};
