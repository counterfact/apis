# Ordergroove simulator

This package combines the Customers, Items, Offers, Orders, Products, and
Subscriptions APIs at their canonical paths in one Counterfact server. For
example, Customers is served at `/customers/` and Products at `/products/`.

The contracts in `openapi/upstream/` are the unchanged published documents and
are used directly by Counterfact.
